# Module Authoring

Use `writing-risum-modules` for `.risum` composition and enable/merge semantics. Exact fields are in `docs/MODULE_FIELDS.md`; embedded syntax is routed through `risu/common/AGENTS.md`.
