# Frozen skill fixtures

These directories are deliberately frozen copies used by the MCP API test harness (`MCP_API_FIXED_SKILL_ROOT`). They are not kept in sync with the real skills under `skills/` and `risu/*/skills/`; edit them only when a test needs a new shape.
