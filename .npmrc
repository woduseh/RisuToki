# Disable npm's progress spinner to prevent PTY output buffering issues
# in non-interactive environments (LLM CLI tools, CI pipelines).
# npm's braille spinner (⠙⠹⠸…) uses ANSI escape codes that interfere
# with PTY process-completion detection on Windows.
progress=false
