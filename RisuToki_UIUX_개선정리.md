# RisuToki UI/UX 개선 정리

> 데스크톱 앱을 직접 사용하며 (메뉴·설정·터미널·가이드 문서, 그리고 `.charx` / `.risup` / `.risum` 전용 편집기) 발견한 개선점을 종합한 문서.
> 검토 환경: Windows 데스크톱 앱, 2026-06-18.

---

## 한눈에 보기

| 우선순위 | 항목                                       | 핵심                                                                             |
| -------- | ------------------------------------------ | -------------------------------------------------------------------------------- |
| 높음     | 메뉴바 두 번 클릭                          | 메뉴 간 전환이 안 되고 닫혔다가 다시 열어야 함                                   |
| 높음     | 빈 관리자 패널 공간 낭비                   | 로어북/에셋이 없으면 빈 패널이 화면 절반을 차지, 편집 영역이 좁아짐              |
| 높음     | "다크 모드 토글"이 페르소나·테마까지 전환  | 명암만 바꾸는 게 아니라 마스코트·색·이름까지 바뀌고, 설정의 테마 드롭다운과 중복 |
| 중간     | 컨트롤 중복·혼재                           | RP/BGM이 설정·헤더 양쪽, 라디오 vs 토글 스위치 혼용                              |
| 중간     | 다크 모드 미적용 영역                      | 상단 메뉴바만 밝은 파란색으로 남음                                               |
| 중간     | 자동 저장 OFF + 저장 상태 표시 부족        | 데이터 손실 위험                                                                 |
| 중간     | 마크다운/문서 프리뷰 부재                  | 번들 가이드 .md가 원본 소스로만 보임                                             |
| 중간     | 불리언 설정이 탭 전체 차지                 | On/Off 하나에 편집 영역 전체 사용                                                |
| 중간     | 이름 변경 모달 + 연필 아이콘 의미          | 옛날식 블로킹 입력창, 연필이 "편집"이 아닌 "이름 변경"                           |
| 중간     | 에셋·파일명 잘림                           | 비슷한 이름 구분 불가, 호버 툴팁 없음                                            |
| 중간     | 프롬프트 관리자 필터 과밀 (.risup)         | 작은 체크박스 20여 개가 3줄로 빽빽                                               |
| 중간     | 프롬프트 블록 정렬이 ↑/↓만 (.risup)        | 드래그 정렬 부재                                                                 |
| 낮음     | 아이콘 버튼 식별성 / 배치 라벨 모호 / 기타 | 다듬기 항목                                                                      |

---

## 1. 높음 — 동작·구조

### 1.1 메뉴바가 두 번 클릭을 요구

- **현상:** 한 메뉴(예: 파일)가 열린 상태에서 인접 메뉴(편집·보기·터미널)를 누르면 *전환*되지 않고 그냥 닫힘. 다시 눌러야 열림. 재현 100%.
- **개선:** 표준 메뉴바처럼 메뉴가 열린 상태에서 다른 상위 메뉴에 hover/클릭 시 즉시 그 메뉴로 전환되도록.

### 1.2 빈 관리자 패널이 공간을 그대로 차지

- **현상:** 편집기 + 로어북 관리자 + 에셋/프롬프트 관리자의 3컬럼 구조에서, 해당 카드·모듈에 로어북·에셋이 없으면 "표시할 항목이 없습니다" 빈 패널이 화면의 절반~3분의 1을 점유. 에셋·로어북이 없는 카드(Kotone)에선 설명 텍스트가 화면 30%만 쓰고 빈 패널 둘이 60%를 차지. `.risum`에서도 에셋이 없으면 우측 3분의 1이 빈 채로 남음.
- **개선:** 비어 있는 관리자 패널은 자동 접기/숨김, 또는 편집기가 남는 폭을 채우도록(반응형). 사용자가 수동으로 다시 펼칠 수 있게.

### 1.3 "다크 모드 토글"이 외형 이상을 바꿈 + 설정과 중복

- **현상:** 보기 > 다크 모드 토글이 명암만 바꾸는 게 아니라 마스코트(토키→아리스), 색 정체성, 터미널 이름(TokiTalk→ArisTalk)까지 통째로 전환. 그런데 설정에는 별도의 테마 드롭다운(토키 라이트 / 아이리스 다크 / 모모톡 / 밀레니엄 / 게헨나 / 트리니티 / 커스텀)이 또 있어서 같은 대상을 건드리는 컨트롤이 둘.
- **개선:** "외형(라이트/다크)"과 "페르소나/테마"를 분리하거나, 토글 이름을 실제 동작에 맞게(예: "페르소나 전환") 변경하고 설정 테마 선택기와 통합.

---

## 2. 중간 — 일관성·효율·안전

### 2.1 컨트롤 중복·혼재

- RP 모드와 BGM이 **설정 모달**과 **TokiTalk 헤더 버튼** 양쪽에 존재 → 정본 위치를 정하거나 상태를 항상 동기화.
- 설정 모달은 토글 스위치, `.risum` 등의 필드는 라디오(On/Off) 사용 → 불리언 컨트롤 형태 통일.

### 2.2 다크 모드가 일부 영역에 미적용

- 다크 모드에서 상단 메뉴바만 밝은 파란색으로 남아 어두운 에디터·패널과 충돌. 메뉴바도 다크 팔레트를 따르도록.

### 2.3 자동 저장 OFF + 저장 상태 표시 부족

- 자동 저장이 기본 OFF이고, 저장되지 않은 변경을 나타내는 표시(탭의 dirty 점 등)가 잘 보이지 않음 → 데이터 손실 위험.
- **개선:** 명확한 "저장 안 됨" 표시, 자동 저장 기본 ON(또는 종료/탭 닫기 시 확인) 고려.

### 2.4 마크다운·문서 프리뷰 부재

- 앱이 문법 가이드 `.md`를 다수 번들로 갖고 있는데 열면 원본 마크다운(표가 `|` 텍스트로)만 보임. 프리뷰(F5)는 `.md`에선 비활성. 자체 문서가 마크다운인 만큼 렌더 프리뷰 또는 분할 뷰 제공.

### 2.5 불리언·짧은 설정이 탭 전체를 차지

- **현상:** `.risum`의 "아이콘 숨김"을 누르면 넓은 가운데 영역에 On/Off 라디오 두 개만 표시. 모듈 정보의 여러 토글이 이런 식이라 설정 하나 바꾸려 탭을 열고 텅 빈 화면을 봐야 함.
- **개선:** 모듈/카드 설정 토글들을 하나의 설정 페이지(폼)로 묶기.

### 2.6 이름 변경이 옛날식 모달 + 연필 아이콘 의미

- 로어북 항목의 연필 아이콘을 누르면 "새 이름:" 블로킹 프롬프트 창이 뜸. 인라인 편집이 더 매끄러움.
- 연필 아이콘이 "내용 편집"이 아니라 "이름 변경"이라 직관과 어긋남 → 아이콘/동작 재정의.

### 2.7 에셋·파일명 잘림

- 에셋 관리자 썸네일 그리드는 좋지만 파일명이 심하게 잘려(`Hari_Daily_a…` 여러 개가 동일해 보임) 호버 툴팁도 없어 구분 불가.
- 참고자료 트리도 전체 상대경로(`bot/...`, `common/...`)를 평평하게 보여줘 긴 이름이 잘림.
- **개선:** 호버 시 전체 이름 툴팁, 이름 2줄 표시, 트리는 폴더로 중첩.

### 2.8 프롬프트 관리자 필터 과밀 (.risup)

- 상단 필터가 타입·색깔·특수 3줄에 작은 체크박스 20여 개로 빽빽 → 접이식 패널이나 칩/드롭다운 형태로 정리.

### 2.9 프롬프트 블록 정렬이 ↑/↓ 버튼만 (.risup)

- 한 칸씩 이동만 가능해 긴 목록에선 번거로움 → 드래그 앤 드롭 정렬 지원.

---

## 3. 낮음 — 다듬기

- **아이콘 버튼 식별성:** 패널·TokiTalk 헤더 아이콘이 작고 비슷함(툴팁은 있어 좋음) → 핵심 동작은 더 큰/명확한 아이콘이나 텍스트 라벨.
- **패널 배치 라벨 모호:** 보기 > 항목 배치의 좌측/우측/좌끝/우끝은 차이를 눌러봐야 앎 → "왼쪽 안쪽/바깥쪽"식 라벨 또는 배치 미리보기 다이어그램.
- **레거시 필드 라벨 모순 (.risup):** "숨겨진 레거시 필드"를 ⚠로 표시하는 건 좋으나 트리 맨 위에 가장 먼저 보이는 건 "숨겨진"과 모순.
- **활성화 모드 컨트롤 (.charx 로어북):** "언제나/강제/선택적"이 3개 체크박스 → 상호배타라면 라디오가 적절.
- **산문 필드도 코드 에디터:** 설명·첫 메시지 등 산문도 줄번호 코드 에디터로 열림(일관성은 있으나 일반 작성자에겐 과함) → 산문용 단순 텍스트 모드 옵션 고려.
- **테마 상태 유지 확인:** 라이트로 바꿔둔 테마가 새 파일을 열면 다크로 바뀌는 듯한 동작 관찰 → 테마가 파일 열기 시에도 유지되는지 점검 권장.

---

## 포맷별 빠른 참조

| 포맷                       | 편집기 성격                        | 강점                                    | 개선 포인트                                                 |
| -------------------------- | ---------------------------------- | --------------------------------------- | ----------------------------------------------------------- |
| `.charx` (캐릭터 카드)     | 항목 트리 + 로어북/에셋 관리자     | 타입별 색 아이콘, 로어북 항목 폼이 깔끔 | 에셋 파일명 잘림, 빈 관리자 공간 낭비, 활성화 모드 체크박스 |
| `.risup` (프롬프트 프리셋) | 프롬프트 블록 리스트               | 레거시 필드 ⚠ 표시, 블록 편집 폼 명확   | 필터 과밀, ↑/↓만 정렬, "숨겨진" 라벨 모순                   |
| `.risum` (모듈)            | 모듈 정보 + 스크립트 + 로어북/에셋 | 로어북 폴더 조직화                      | 불리언이 탭 전체 차지, 빈 에셋 패널 낭비                    |

---

## 유지할 강점

- 빈 상태 안내가 명확함("파일을 열어주세요 (Ctrl+O)", "항목을 선택하세요").
- 도움말 단축키/기능 시트, 아이콘 호버 툴팁 제공.
- 터미널 명령어 구문 강조, AI CLI(Claude Code/Copilot/Codex/Gemini) 통합.
- 항목 트리의 타입별 색 아이콘, 로어북 폴더 조직화, 블루아카이브 테마 페르소나 등 매력적인 디테일.
